# Untracked input data and generated results (per project convention)
/data/
/results/

# Rendered notebook (regenerate from nil_introgression_1400.qmd)
/nil_introgression_1400.html
/nil_introgression_files/
/nil_introgression.knit.md

# R session state
.RData
.Rhistory
.Rproj.user/
*.Rproj

# Claude Code local state
/.claude/

# macOS
.DS_Store

# agent/ — internal working area (handoff prompts, roadmaps); not published
/agent

/.quarto/
**/*.quarto_ipynb

# Quarto render artifacts: freeze/exec caches (regenerated) and the stray per-figure
# _files dirs from embed-resources docs (self-contained HTML needs no sidecar dir).
# NB: docs/sample_correspondence_files/ is intentionally tracked (non-embed doc), so
# only the specific bc2s2 _files dirs are ignored, not a blanket *_files/.
*_cache/
/bc2s2_vs_bc2s3_files/
/docs/bc2s2_vs_bc2s3_files/
